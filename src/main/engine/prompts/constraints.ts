// 📁 路径：src/main/engine/prompts/constraints.ts

/**
 * 强制约束字典 (Constraints)
 * 用于防止大模型"越狱"、格式错乱或合并长句
 */
export const CONSTRAINTS = {
  JSON_ONLY: `【致命约束】：请只输出合法的 JSON 格式数据，不要包含任何 Markdown 标记（如 \`\`\`json ）、不要包含任何解释性文本、前言或后语。`,
  NO_MERGE_SENTENCES: `【高颗粒度警告】：绝不允许把多句话合并成一个长句子！你必须像真正的剪辑师一样，主动将台词按自然的"断句"、"标点符号"或"呼吸口"进行物理拆分。每一句短句必须是一个独立的 JSON 对象！`,
  PRESERVE_ID: `【溯源协议】：必须严格保留并返回输入的 sourceShotId，绝不允许篡改或丢失，否则系统将无法对齐画面。`,

  /** 反看图说话准则：禁止复述画面已直观呈现的动作 */
  ANTI_LITERAL: `【反看图说话准则】：绝不允许复述画面已直观呈现的表面动作！当上游输入"动作[拿起水杯] 情绪[脸色阴沉]"时，文案不能写"他拿起水杯喝了一口水"，而必须转化为言下之意与内心戏："他喝下的不是水，而是将最后一口怒火硬生生压回了肚子里。"重点剖析人物动机、情绪压抑与剧情转折，而非描绘可见动作。`,

  /** TTS口语化准则：为下游语音合成优化句式 */
  TTS_FRIENDLY: `【TTS口语化准则】：文案必须适合语音朗读，严禁出现超过25字没有标点的长句。多用短句、逗号、感叹号。在关键情绪转折处插入停顿标记，例如：[pause: 500ms]。在情绪突变处插入语气标记，例如：[tone: shocked]。这些标记不会被朗读，但会指导TTS引擎调整节奏与情感。`,

  /** 角色别名轮换准则：避免全篇重复真名导致听觉疲劳 */
  ROLE_ALIAS: `【角色别名轮换准则】：严格基于人物库指代角色，严禁使用"男子/女子/青年/中年人"等模糊代称。但在真实姓名与合乎情理的身份代词之间智能轮换，避免全篇重复同一称呼导致听觉疲劳。例如同一角色可在"高启强"、"老高"、"这个卖鱼佬"、"京海未来的地下皇帝"之间根据上下文切换，既保证指代精准又丰富表达。`,
};
